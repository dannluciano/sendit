# sendit

1. No Linux ou no Windows SubfileSystem Linux (WSL) rode o script de setup:
```./setup.sh```
2.  Rode o servidor web
    em modo de desenvolvimento:
```pipenv run dev```
    em modo de produção:
```pipenv run start```
3. Abra o navegador na url 
```localhost:5100/```
