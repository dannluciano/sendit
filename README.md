# sendit
SendIT

1. Se não estiver o pip instalado execute: python get-pip.py install
2. Após isto, instale o virtualenv: pip install virtualenv
3. Crie um virtualenv com o comando python3 -m venv env
4. pip install -r requeriments.txt
5. Depois entre na pasta SendIT e execute: python manage.py runserver
